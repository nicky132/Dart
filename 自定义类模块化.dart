import 'lib/Person.dart';
void main(){
  // Person p = new Person("李",2);
  // print(p.name);
  // p.getInfo();

  // Person p1=new Person.now();
  // print(p1);

  Person p2 = new Person.serInfo("王三", 8);
  p2.getInfo();
  
}